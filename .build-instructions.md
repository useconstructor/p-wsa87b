The Next.js project already has all files pre-generated and written to disk.
Pre-generated files (already in the repo):
  - app/page.tsx
  - app/globals.css
  - app/layout.tsx
DESIGN TARGET (reference mockup): https://vabacntbfcwhgkjqajcw.supabase.co/storage/v1/object/public/screenshots/projects/P-WSA87B/design-mockup.png
The pre-generated files implement this design. Do NOT redesign — only fix build errors.
YOUR ONLY JOB:
1. Run: npm run build
2. If it passes with no errors: commit all files and push. Done.
3. If there are TypeScript or JSX build errors: fix ONLY the broken lines, then rebuild.
⛔ DO NOT rewrite or redesign files that already exist
⛔ DO NOT add new sections or change the design
⛔ DO NOT remove content — only fix syntax/type errors
✓ If npm run build passes, commit and push immediately
Original context (for reference only — do not rewrite based on this):
Project: Project 1788534251307
Business: project management SaaS
═══ INTERACTIVE FORMS (apply even in fix mode) ═══
If any contact/booking/quote/newsletter form exists in the pre-generated files, ensure it is wired up:
- POST to: `${process.env.NEXT_PUBLIC_CONSTRUCTOR_API}/v1/forms/${process.env.NEXT_PUBLIC_PROJECT_ID}`
- JSON body with all form field values
- Loading state ("Enviando…"), success message, error handling
- 'use client' directive on the component
If no form exists but the business type would benefit from one (contact, booking, quote), ADD a simple contact form section.
